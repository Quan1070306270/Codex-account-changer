# 安全说明

本项目会代表部署者保存并使用 ChatGPT/Codex 登录凭据。请将管理站、服务器和备份视为高敏感系统。

## 支持范围

- 只在自己控制的服务器上部署。
- 只添加本人拥有并有权使用的账号。
- 不要将账号数据、Docker 数据卷、`.env`、访问密钥或备份提交到 Git。
- 不要直接向公网开放 `3000` 或 `3210` 端口。

## 部署要求

- 为 `ADMIN_PASSWORD` 设置高强度密码。
- 使用 `openssl rand -hex 32` 生成 `SESSION_SECRET`。
- 保持 Caddy HTTPS 正常，并限制 SSH 登录来源。
- 定期备份 `account_data` 和 `caddy_data`，备份文件应设置严格权限。
- 服务器或备份疑似泄露时，应立即重新登录所有账号并轮换管理密码。

账号授权数据保存在部署者自己的 Docker 数据卷中，默认依赖宿主机权限隔离，并未进行应用层加密。获得服务器 root 权限或未受保护备份的人可能读取这些数据。

## 漏洞报告

请不要在公开 Issue 中粘贴真实 Token、密码、Cookie、授权文件、服务器地址或日志原文。报告问题时只提供脱敏后的复现步骤，并先移除所有账号标识和密钥。

建议通过 GitHub Security Advisory 的私密报告功能提交安全问题；如果仓库未启用该功能，请仅提交不包含任何秘密的最小问题描述。
